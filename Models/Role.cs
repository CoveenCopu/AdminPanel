namespace AdminPanel.Models
{
    // Enum som definerer brugerroller i systemet
    public enum Role
    {
        // Almindelig bruger
        User,

        // Administrator med fuld adgang
        Admin
    }
}